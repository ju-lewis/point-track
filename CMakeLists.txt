# Configure CMake
cmake_minimum_required(VERSION 3.5)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)


# Define component directories
set(EXTRA_COMPONENT_DIRS "device"
    C:/Users/booll/esp-idf-lib/components)


project(device)
